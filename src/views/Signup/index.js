import React from 'react';
import { Layout, Button } from '@ui-kitten/components';

export default (props) => {
  return (
    <Layout>
      <Button>
        Signin
      </Button>
    </Layout>
  )
}